var Fluxxor = require("fluxxor");
var ConstantsUser = require("../constants/user");

var UserStore = Fluxxor.createStore({

  initialize: function() {
    this.bindActions(
    );
  }

});

module.exports = UserStore;
