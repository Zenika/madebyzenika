apiVersion: v1
kind: Service
metadata:
  labels:
    name: rest
  name: rest
spec:
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    name: spring
